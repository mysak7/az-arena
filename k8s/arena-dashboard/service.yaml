apiVersion: v1
kind: Service
metadata:
  name: arena-dashboard
  namespace: arena
  labels:
    app: arena-dashboard
spec:
  selector:
    app: arena-dashboard
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
